# Mac-compatible dependencies for local dataset download (src/download_dataset.py)
# Use ONLY inside a project virtual environment — never install globally on macOS.
# Install: pip install -r requirements.txt

# Datasets (Hugging Face) — only direct third-party import in download_dataset.py
datasets>=2.18.0,<3.0.0

# Core transitive dependencies (pinned for macOS + Python 3.8 compatibility)
huggingface-hub>=0.23.0
pyarrow>=14.0.0,<18.0.0
tqdm>=4.66.0
numpy>=1.21.0,<1.25.0
pandas>=1.3.0,<2.2.0
requests>=2.31.0
filelock>=3.13.0
packaging>=23.0
fsspec>=2023.1.0
dill>=0.3.0
multiprocess>=0.70.0
