# Full project dependencies (V1 dataset · V2 fine-tuning · V3 RAG demo)
# Install: pip install -r requirements.txt

# ---------------------------------------------------------------------------
# Dataset download (download_dataset.py, create_instruction_dataset.py)
# ---------------------------------------------------------------------------
datasets>=2.18.0,<3.0.0
huggingface-hub>=0.23.0
pyarrow>=14.0.0
tqdm>=4.66.0
numpy>=1.24.0
pandas>=2.0.0
requests>=2.31.0
filelock>=3.13.0
packaging>=23.0
fsspec>=2023.1.0
dill>=0.3.0
multiprocess>=0.70.0

# ---------------------------------------------------------------------------
# PyTorch & LLM fine-tuning (train.py, train_instruction.py, infer_instruction.py)
# ---------------------------------------------------------------------------
torch>=2.1.0
transformers>=4.40.0,<4.46.0
tokenizers>=0.19.0,<0.20.0
safetensors>=0.4.0
peft>=0.10.0
accelerate>=0.28.0
bitsandbytes>=0.43.0; platform_system == "Linux"
bitsandbytes>=0.41.0; platform_system != "Linux"
trl>=0.8.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# ---------------------------------------------------------------------------
# Evaluation
# ---------------------------------------------------------------------------
evaluate>=0.4.0
scikit-learn>=1.3.0

# ---------------------------------------------------------------------------
# V3 RAG (build_rag_index.py, rag_retrieve.py, app.py)
# ---------------------------------------------------------------------------
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
streamlit>=1.28.0
