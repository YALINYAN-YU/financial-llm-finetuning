# Training dependencies for Colab / Kaggle (GPU environments)
# Not required for local dataset download on Mac.
# Install: pip install -r requirements-train.txt

-r requirements.txt

# PyTorch (install platform wheel separately on Colab/Kaggle if needed)
torch>=2.1.0

# Hugging Face Transformers
transformers>=4.40.0
tokenizers>=0.19.0
safetensors>=0.4.0

# PEFT (Parameter-Efficient Fine-Tuning / LoRA)
peft>=0.10.0

# Accelerate
accelerate>=0.28.0

# BitsAndBytes (4-bit quantization for QLoRA — Linux/CUDA only)
bitsandbytes>=0.43.0

# Evaluate (Hugging Face metrics)
evaluate>=0.4.0

# Scikit-learn
scikit-learn>=1.3.0

# TRL (supervised fine-tuning helpers)
trl>=0.8.0

# Tokenizer / model extras
sentencepiece>=0.2.0
protobuf>=4.25.0
